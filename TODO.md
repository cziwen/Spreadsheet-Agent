# Fixes
- refactor CLI so that user can run a script to enter cli mode and start chatting with agent
- check what `python cli.py demo` does
- **fix `python cli.py scenario`**

# Test Queries
- "Create a scenario where all order amounts increase by 10%"
- "Create a scenario where high-value orders (>200) increase by 20% but small orders by 5%"
- "List all saved scenarios"

# Features
- use tools
- session management
- context awareness